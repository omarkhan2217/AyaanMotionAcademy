import React from "react"
import { FoundationBatchContainer} from "../containers"
import { Layout } from "../layout"

export const Foundationbatch: React.FC = () => {
    return (
        <Layout>
            <FoundationBatchContainer />
        </Layout>
    )
}