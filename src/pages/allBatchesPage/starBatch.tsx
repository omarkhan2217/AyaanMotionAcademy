import React from "react"
import { StarBatchContainer} from "../../containers"
import { Layout } from "../../layout"

export const StarBatch: React.FC = () => {
    return (
        <Layout>
            <StarBatchContainer />
        </Layout>
    )
}

export default StarBatch;