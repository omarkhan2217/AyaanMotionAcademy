import React from "react"
import { ScholarBatchContainer} from "../../containers"
import { Layout } from "../../layout"

export const ScholarBatch: React.FC = () => {
    return (
        <Layout>
            <ScholarBatchContainer />
        </Layout>
    )
}

export default ScholarBatch;