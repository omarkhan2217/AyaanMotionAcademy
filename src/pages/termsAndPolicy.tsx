import React from "react"
import { Layout } from "../layout"
import {  TermsAndPolicycontainer } from "../containers"

export const TermsAndPolicy: React.FC = () => {
    return (
        <Layout>
            < TermsAndPolicycontainer/>
        </Layout>
    )
}

export default TermsAndPolicy;